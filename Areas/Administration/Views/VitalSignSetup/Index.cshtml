@model IEnumerable<HMS.Models.Administration.VitalSignSetup>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_HMSMainLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitOfMeasurement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpdatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitOfMeasurement.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdatedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VitalSignSetupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VitalSignSetupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VitalSignSetupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
